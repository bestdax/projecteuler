#pragma once
#include <iostream>
#include <vector>
#include <algorithm>

class Solution
{
public:
	unsigned reciprocal(unsigned n);
	unsigned number_has_longest_reciprocal_cycles_within(unsigned cap);
	void answer();

};
