#pragma once
#include <iostream>
#include "longint.h"

class Solution
{
public:
	lint self_power(unsigned n);
	lint last_ten_digits_of_self_power(unsigned n);
	lint sum_of_self_power(unsigned cap);
	void answer();

};
