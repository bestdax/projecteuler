#pragma once
#include <iostream>
#include <vector>

class Solution
{
	public:

		std::vector<short> self_power(unsigned n);
		std::vector<short> sum_of_self_powers(unsigned n);
		void print_self_power(std::vector<short>);
		void answer();

};
