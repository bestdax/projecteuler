#pragma once
#include <iostream>
#include <cmath>

class Solution
{
public:
	bool is_abundant(unsigned n);
	bool is_sum_of_two_abundants(unsigned n);
	unsigned non_abundant_sums();
	void answer();

};
