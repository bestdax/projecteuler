#pragma once
#include <iostream>
#include <string>
#include <vector>


class Solution
{
public:
	unsigned collatz_count(unsigned long start_number);
	unsigned long longest_chain_within(unsigned long range);
	void answer();

};
