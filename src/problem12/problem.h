#pragma once
#include <iostream>
#include <cmath>


class Solution
{
public:
	unsigned long nth_triangle_number(unsigned n);
		unsigned number_of_divisors(unsigned long n);
	unsigned long triangle_number_with_over_n_divisors(unsigned n);
	void answer();

};
