#pragma once
#include <iostream>
#include <vector>

class Solution
{
public:
	std::vector<unsigned long> prime_permutations(unsigned long n);
	std::vector<unsigned long> pick_arithmetic_sequence(std::vector<unsigned long> numbers);
	void answer();

};
