#pragma once
#include <iostream>
#include <vector>

class Solution
{
public:
	std::vector<unsigned long> gen_prime_list(unsigned long cap);
	unsigned long consequtive_prime_sum(unsigned long cap);
	void answer();

};
