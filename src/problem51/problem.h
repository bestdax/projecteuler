#pragma once
#include <iostream>
#include "number.h"
#include <string>
#include <vector>
#include <algorithm>

class Solution
{
	public:
		std::vector<unsigned long> replace_digit_and_get_primes(unsigned long n);
		void answer();

};
