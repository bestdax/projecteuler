#pragma once
#include <iostream>
#include <cmath>

class Solution
{
public:
	unsigned sum_of_proper_divisors(unsigned n);
	bool has_amicable(unsigned n);
	unsigned sum_of_amicables(unsigned long n);
	void answer();

};
