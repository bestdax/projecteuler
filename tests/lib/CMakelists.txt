if(BUILD_TESTING)
  get_filename_component(DIR_NAME ${CMAKE_CURRENT_SOURCE_DIR} NAME)
  set(EXENAME ${DIR_NAME}_test)
  add_executable(${EXENAME} number_test.cpp)
  add_executable(bint_test bint_test.cpp)
  target_link_directories(${EXENAME} PUBLIC ${PROJECT_BINARY_DIR}/lib/)
  target_link_libraries(${EXENAME} gtest_main number)
target_link_libraries(bint_test gtest_main bint)
  include(GoogleTest)
  gtest_discover_tests(${EXENAME})
  gtest_discover_tests(bint_test)
endif()
